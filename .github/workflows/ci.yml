name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: spuri_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d spuri_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/spuri_test?sslmode=disable
      FINANCE_ENCRYPTION_KEY: ci-finance-encryption-key-at-least-32
      RUN_POSTGRES_INTEGRATION: "1"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: 1.24.12
      - run: go build ./...
      - run: go vet ./...
      - run: go test ./...
