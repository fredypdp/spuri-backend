# ============================================
# SPURI API - VARIÁVEIS DE AMBIENTE
# ============================================

# Banco de Dados
DATABASE_URL=postgresql://user:password@host:5432/spuri_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=senha
DB_NAME=spuri_db
DB_SSLMODE=disable

# Servidor
PORT=8080
ENV=development

# JWT
JWT_SECRET=sua-chave-secreta-aqui-minimo-32-caracteres
JWT_EXPIRY_HOURS=24

# 🔥 GOOGLE SMTP (Email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
SMTP_PASSWORD=sua-senha-de-app-do-gmail
FROM_EMAIL=noreply@spuri.ao
FROM_NAME=Spuri - Sistema Académico

# URL Base (para links nos emails)
BASE_URL=https://api.spuri.ao

# Lista separada por vírgula de origens permitidas (CORS)
ALLOWED_ORIGINS=http://localhost:3000,https://spuripainel.vercel.app,https://spuri.ao

# =============================================================================
# RATE LIMITING
# =============================================================================

# Requisições por minuto (global)
RATE_LIMIT_GLOBAL=100

# Tentativas de login por minuto
RATE_LIMIT_LOGIN=5

# Envios de email por hora
RATE_LIMIT_EMAIL=2

# =============================================================================
# LOGGING
# =============================================================================

# Nível de log (debug, info, warn, error)
LOG_LEVEL=info

# Formato (json, text)
LOG_FORMAT=json

# =============================================================================
# FEATURES FLAGS
# =============================================================================

# Ativar web search
ENABLE_WEB_SEARCH=false

# Ativar email verification
ENABLE_EMAIL_VERIFICATION=true

# Ativar password recovery
ENABLE_PASSWORD_RECOVERY=true