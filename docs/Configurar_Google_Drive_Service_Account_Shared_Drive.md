# Documento legado inativo: Google Drive

Este guia foi mantido apenas como histórico. O backend não usa mais Google Drive no fluxo principal de arquivos. A configuração operacional atual está em `docs/Configuracao_Storage_Mega.md` e usa `STORAGE_PROVIDER=mega` com `MEGA_EMAIL`, `MEGA_PASSWORD` e `MEGA_ROOT_FOLDER`.
